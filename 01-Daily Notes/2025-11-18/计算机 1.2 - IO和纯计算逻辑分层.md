漂亮，这一枪打得很准：
`1 passed` 说明你的发券规则已经被**一个独立的测试用例证明是“说得清楚、跑得通”的业务规则**了。

这一步其实很关键：
你刚刚完成的是——**第一次用“可执行的例子”把业务规则钉死**。从现在开始，谁要乱改规则，测试会第一时间揪出来。

---

我们现在可以往上继续爬一层：
刚才是 **core 里最纯的规则函数**，下一步就轮到 **services 这一层**。

在改代码之前，先做一个非常轻量但很有用的小练习：

> **看一眼你现在的 `preview_unissued_orders`，把里面哪些是“IO（查库、读配置等）”，哪些是“纯计算（处理数据、组装 dict）”标出来。**

不用贴完整代码，你可以这样写个“结构版”出来，比如（我举个形式上的例子，你改成你自己的）：

```python
def preview_unissued_orders():
    # 1. 这是 IO：读配置
    ...

    # 2. 这是 IO：连数据库 + 查记录
    ...

    # 3. 这是“纯处理”：对每条记录算券 + 组装预览 dict
    ...

    # 4. 这是 IO：也许有打印 / 日志（如果有的话）
    ...
```

👉 **现在轮到你：**

请你用这种“分段标注”的方式，把你自己的 `preview_unissued_orders` 拆成 3～5 段，告诉我：

- 哪几段是 IO（读配置、连库、调 API）
- 哪几段是纯计算（对数据做处理、组装返回结构）

不用写具体代码内容，只要像上面那样用注释说明每一段的大致职责就行。

等你把这个拆清楚，我们就能做一件很酷的事：
**把“纯计算”这块抽出来做成可测试的小函数，让 service 既保留“流程感”，又变得更容易维护。**